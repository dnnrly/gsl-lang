# Draft 01 — generated by an LLM from prose-spec.md in one pass.
# Intentionally NOT yet validated. This is the raw first attempt.
# The next step in the experiment drives this file through the GSL parser.

@critical: promo-service, rules-service

node promo-service [team="promo"]
node rules-service [team="merchandising"]
node offer-db [team="merchandising"]
node catalog-service [team="storefront"]
node catalog-cache [team="storefront"] @deprecated
node legacy-promo-calc [team="promo"]
node returns-system [team="fulfilment"]
node reward-ledger [team="accounting"]
node event-bus [team="platform"]
node main-product-db [team="platform"]

promo-service -> offer-db [protocol="sql"]
promo-service -> rules-service [protocol="grpc"]
promo-service -> reward-ledger [instrument="settlement"]
promo-service -> event-bus [protocol="pub", instrument="settlement"]
legacy-promo-calc -> main-product-db [protocol="sql"]
legacy-promo-calc -> catalog-cache [protocol="tcp"]
returns-system -> legacy-promo-calc [protocol="internal"]
catalog-service -> offer-db [protocol="sql"]
catalog-service -> catalog-cache [protocol="tcp"]